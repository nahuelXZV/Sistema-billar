@using WebClient.Components
@using WebClient.Components.Inventory.TraspasoInventario
@using WebClient.Models.Inventory
@model TraspasoInventarioViewModel

@{
	ViewData["Title"] = "Traspasos";
	ViewData["Module"] = "Inventario";
	ViewData["Action"] = "Crear";
	ViewData["Url"] = Url.Action("Listado", "TraspasoInventario");

	var componentName = nameof(TraspasoInventarioCrearComponent);
	var componentParameters = new Dictionary<string, object>
	{
		{ nameof(TraspasoInventarioCrearComponent.Traspaso), Model.Traspaso },
		{ nameof(TraspasoInventarioCrearComponent.ListadoAlmacenes), Model.ListadoAlmacenes }
	};
}

<script>
	class @componentName {
		constructor(componentInstance) {
			this.componentInstance = componentInstance;
			document.getElementById('btn-save').addEventListener('click', () => this.Validar());
		}

		async Validar() {
			await this.componentInstance.invokeMethodAsync('Validar');
		}

		static init(componentInstance) {
			window.@componentName = new @(componentName)(componentInstance);
		}
	}

	window.@componentName = @componentName;
</script>

@section ActionBar {
	<div class="iu-tools">
		<div class="iu-tools-actions">
			<button type="button" class="iu-btn iu-btn-secondary" onclick="history.back()">
				<i class="bi bi-arrow-left"></i>
				Volver
			</button>
			<button id="btn-save" type="button" class="iu-btn iu-btn-primary">
				<i class="bi bi-floppy"></i>
				Guardar
			</button>
		</div>
	</div>
}

<section class="iu-show">
	<component type="typeof(MainComponent)"
			   param-ComponentTypeName="typeof(TraspasoInventarioCrearComponent).FullName"
			   param-Parameters="@componentParameters"
			   render-mode="ServerPrerendered" />
</section>
